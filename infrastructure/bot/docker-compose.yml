services:
  bot:
    build:
      context: ../..
      dockerfile: Dockerfile.bot
    container_name: riverai-bot
    env_file:
      - ../../.env
    restart: unless-stopped
    networks:
      - riverai-network

networks:
  riverai-network:
    external: true
